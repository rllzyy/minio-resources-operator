apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        imagePullPolicy: Always
        env:
          - name: VAULT_ADDR
            value: http://vault.default:8200/
          - name: VAULT_TOKEN
            value: s.TgfzLPO574M0vrS7NcQFkUFt
