apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        env:
          - name: VAULT_ADDR
            value: http://vault.default:8200/
          - name: VAULT_TOKEN
            value: derp
